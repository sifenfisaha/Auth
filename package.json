{
  "name": "sifen-auth-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -ws",
    "test": "npm run test -ws",
    "lint": "npm run lint -ws",
    "format": "npm run format -ws"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
